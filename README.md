# projeto-disciplina-trimestre-Wesley-Gomes
git config --global --replace-all user.name wesleygomes30
git config --global --replace-all user.email wesley.gomes30@escola.pr.gov.br
git add .
git comit -m "Primeiro comit"
giy push
